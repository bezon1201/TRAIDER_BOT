Version: 1
- Каркас FastAPI (app.py) с GET/HEAD для / и /health (для UptimeRobot).
- На старте пинг Binance через /api/v3/account (HMAC) для проверки ключа.
- Отправка в ADMIN_CHAT_ID: "<UTC> Бот запущен\nBinance connection: ✅/❌".
- Используются переменные из Environment Variables.txt (прокси, токен, admin chat, ключи). Новых переменных не добавлял.
- Совместимо с:
  Build Command=pip install -r requirements.txt
  Start Command=uvicorn app:app --host 0.0.0.0 --port $PORT

Version: 2
- Добавлен Telegram webhook: на старте сверяем getWebhookInfo и ставим setWebhook на WEBHOOK_BASE + '/tg'.
- Обработчик /tg принимает POST от Telegram и отвечает на /start и любые тексты.
- Уведомление админу теперь включает статус вебхука (Webhook: ✅/❌).
- Прокси берутся из HTTP_PROXY/HTTPS_PROXY из окружения (как и раньше).
- Остальное без изменений: HEAD для / и /health, Binance ping на старте.

Version: 9
- Сообщения-карточки теперь моноширинные: <pre> + parse_mode=HTML.
- Убран вывод тройных кавычек из /portfolio.

Version: 6
- Fix: добавлен httpx в requirements и закреплён на 0.27.2, чтобы работал параметр `proxies`.
- /portfolio: сообщение целиком отдаётся в моноширинном блоке (``` ... ```).
- Нормализация активов: LDUSDC учитывается как USDC в категории Earn/стейблы.
- Сортировка: BTC, ETH сверху; далее остальные монеты; стейблы внизу.
- Проценты распределения считаются без учёта стейблов (spot_only %).


Version: 7
- Удалены импорты несуществующих модулей (metrics_runner и пр.).
- Каркас FastAPI + Telegram webhook; команда /portfolio.
- Вывод в моноширинном виде через <pre>; сортировка BTC/ETH → альты → стейблы.
- Нормализация: LDUSDC → USDC; % считаются без стейблов.
- requirements: добавлен httpx==0.27.2.
