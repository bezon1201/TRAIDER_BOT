11
- Исправлен формат карточки /portfolio: вывод строго моноширинным блоком с тройными апострофами (```).
- Больше никаких литералов \n в тексте: отправляем реальные переводы строк.
- Нормализация LDUSDC → USDC (суммируется к стейблам).
- Игнор активов, суммарная стоимость которых < 1 USD.
- BTC/ETH первыми, затем альты по убыванию USD.
- Отдельные блоки: FIAT, STABLES / EARN.
- Цифры выравнены: столбец с $ ровно под $.
- httpx берёт proxy из переменных окружения (trust_env=True).
- Добавлен requirements.txt (httpx, fastapi, uvicorn).
