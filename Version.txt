Version: 1
- Каркас FastAPI (app.py) с GET/HEAD для / и /health (для UptimeRobot).
- На старте пинг Binance через /api/v3/account (HMAC) для проверки ключа.
- Отправка в ADMIN_CHAT_ID: "<UTC> Бот запущен\nBinance connection: ✅/❌".
- Используются переменные из Environment Variables.txt (прокси, токен, admin chat, ключи). Новых переменных не добавлял.
- Совместимо с:
  Build Command=pip install -r requirements.txt
  Start Command=uvicorn app:app --host 0.0.0.0 --port $PORT

Version: 2
- Добавлен Telegram webhook: на старте сверяем getWebhookInfo и ставим setWebhook на WEBHOOK_BASE + '/tg'.
- Обработчик /tg принимает POST от Telegram и отвечает на /start и любые тексты.
- Уведомление админу включает статус вебхука (Webhook: ✅/❌).
- Прокси берутся из HTTP_PROXY/HTTPS_PROXY.

Version: 3
- Исправлено падение на старте: добавлен import json (NameError).
- Проверка и установка вебхука не блокируют запуск; статус уходит админу.
- Формат Version.txt: вся история в одном файле, новые версии добавляем ниже.

Version: 4
- Команда /portfolio: формирование ответа строго по шаблону из PORTFOLIO.txt (моношрифт, выравнивание).
- Данные: Spot из /api/v3/account; Earn из Simple Earn (flexible/locked). Цены — спотовые (USDT/USDC/FDUSD/BUSD), для стейблов 1.0.
- Сортировка Spot: BTC, ETH сверху; затем прочие не-стейблы по USD ↓; стейблы внизу. Проценты только для не-стейблов от суммы не-стейблов.
- Фильтр строк: скрываю позиции < 1.00$.
- Итог: Total/Invested/Profit с стрелкой без пробела перед процентом.

Version: 5
- Ресборка: без изменений логики. Файлы: app.py, portfolio.py, requirements.txt, Version.txt.
