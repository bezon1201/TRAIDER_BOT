BOT COMMANDS (short reference)
==============================

General
-------
/start               — ping, healthcheck
/now                 — обновить данные по всем core монетам и вывести карточки
/json                — список json в /data; c аргументом — скачать файл

Portfolio
---------
/portfolio           — сводка портфеля (core сначала, stable в конце)

Budget (core LONG)
------------------
/budget                      — показать бюджеты (по одной монете — отдельным сообщением)
/budget <symbol>=<N>         — weekly бюджет (целое), напр. /budget btcusdc=100
/budget <symbol>+<N>         — прибавить к weekly
/budget <symbol>-<N>         — вычесть из weekly
/budget reset                — «как начало месяца/первой недели» (сброс ролловеров и спендов)
/budget schedule             — интервалы недели/месяца и TZ

Scheduler
---------
/sched status                — статус планировщика (enabled, interval, jitter, last_run, last_ok, updated_last)
/sched on                    — включить фоновый пересчёт (как /now, без постов)
/sched off                   — выключить фоновый пересчёт
/sched run                   — разовый прогон
/sched set interval=60 jitter=3  — сменить интервал (сек) и джиттер (сек)

Trading (подготовка)
--------------------
/orders preview [SYMBOL] [--risk=green|yellow|all]
/orders place   [SYMBOL] [--risk=green|yellow|all]
/orders cancel
/trade on|off

Notes
-----
- Флаги: 🟢 — уровень пройден → MARKET; 🟡 — можно ставить LIMIT; 🔴 — новый ордер не открывать.
- Карточки /now содержат W/M и left для TP/L0/L1/L2/L3.
- Все суммы — целые числа.
