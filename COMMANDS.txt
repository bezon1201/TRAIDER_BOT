# COMMANDS (full list, inferred from sources)

## Top-level commands detected:
/budget
/coins
/data
/invested") or text_lower.startswith("/invest 
/market
/mode
/now
/portfolio
/sheduler

## Grouped usage
/budget
  subcommands: cancel, oco, open, rolover, start

/coins

/data
  subcommands: delete

/invested") or text_lower.startswith("/invest 

/market

/mode

/now
  subcommands: long

/portfolio
  subcommands: pairs

/sheduler
  subcommands: on

## Cheat sheet (curated)
# COMMANDS (cheat sheet)

## General
/now                         # показать карточки всех пар
/<pair>                      # карточка конкретной пары (например /btcusdc)
/bot status                  # текущий режим бота
/bot auto|manual|pause       # включить автоторговлю / ручной режим / пауза (запрет OPEN)

## Budget (глобальные)
/budget start                # установить карманы по базовым процентам текущего рынка (budget × база)
/budget rolover              # закрыть ручные флаги → AUTO, пересчитать карманы; резервы = 0; spent НЕ трогать
/budget cancel               # конец месяца: обнулить budget, pockets, flags, reserve, spent

## Budget (пара и ордера)
/budget <pair>=<N>           # установить бюджет пары (например: /budget btcusdc=100)

# Флаги/ордеры (ручные)
/budget <pair> <ord> open    # поставить ⚠️: зарезервировать сумму кармана и открыть ордер (проверка лимита)
/budget <pair> <ord> fill    # выполнить ✅: перевести резерв → spent, установить флаг fill
/budget <pair> <ord> cancel  # снять ⚠️: вернуть резерв, флаг → AUTO
# ord ∈ {OCO, L0, L1, L2, L3}

## Примечания
- Проверка лимита при OPEN: spent.total + reserve.total + pocket_amt(ord) ≤ budget.
- В режиме PAUSE: любые OPEN запрещены (ботом и вручную).
- В режиме MANUAL: бот сам не открывает ордера.
- В /budget rolover: резервы нулим, spent сохраняем.
- В карточке вторая строка показывает: SYMBOL BUDGET | ⏳reserve.total | 💸spent.total
