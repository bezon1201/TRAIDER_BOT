# data_module: placeholder of data commands (kept minimal in this patch)
# The real logic remains in earlier iterations; here we keep stubs to not break imports.
from aiogram import Router
router = Router()
