# Telegram Bot - FastAPI Version 4.5

**Полная переделка архитектуры сбора метрик!**

## Версия 4.5 (12.11.2025)

✨ **Унифицированный сбор всех метрик в одном месте:**

- **Архитектура:** Все данные собираются одновременно в одной функции `collect_metrics_for_symbol()`
- 📊 **Данные на пару:** ticker + filters + klines (12h, 6h, 4h, 2h) + SMA14/ATR14
- 💾 **Сохранение:** Один JSON файл на пару (например, ETHUSDT.json) со всеми данными
- 🎯 **Торговые фильтры:**
  - `price_filter`: min_price, max_price, tick_size
  - `lot_size`: min_qty, max_qty, step_size
  - `min_notional`: минимальная стоимость сделки
  - `max_num_orders`, `max_num_algo_orders`: лимиты ордеров
- 🔇 Сбор молча без сообщений в Telegram

## Структура JSON (правильная)

```json
{
  "symbol": "ETHUSDT",
  "timestamp": "2025-12-12T02:22:00+00:00",
  "ticker": {
    "price": 3450.36,
    "bid_price": 3450.24,
    "ask_price": 3450.25,
    "high": 3635.46,
    "low": 3403.99,
    "volume": 139750.4754,
    "trades": 635339
  },
  "filters": {
    "price_filter": {
      "min_price": 0.01,
      "max_price": 1000000,
      "tick_size": 0.01
    },
    "lot_size": {
      "min_qty": 0.001,
      "max_qty": 10000,
      "step_size": 0.0001
    },
    "min_notional": {
      "min_notional": 10
    },
    "max_num_orders": {
      "limit": 200
    }
  },
  "timeframes": {
    "12h": {
      "klines": [...100 свечей...],
      "indicators": {
        "sma14": 3441.125,
        "atr14": 140.20
      }
    },
    "6h": {...},
    "4h": {...},
    "2h": {...}
  }
}
```

## Команды бота

### `/start`
Приветствие

### `/coins <pair1> <pair2> ...`
Добавляет пары

### `/now`
Собирает **ВСЕ метрики** (ticker + filters + klines + indicators) одновременно и молча

### `/data`
Список файлов

### `/data export all`
Отправляет все файлы

### `/data delete all`
Удаляет все файлы

## Ключевые изменения в 4.5

✅ **Фильтры встроены в основной сбор метрик** (не отдельный модуль)
✅ **Один вызов = все данные** (ticker + filters + свечи + индикаторы)
✅ **Один JSON файл = полные метрики** для каждой пары
✅ **Архитектура соответствует старому боту**

## Кеширование фильтров

Фильтры кешируются на **6 часов**, поэтому:
- Первый запрос `/now` - медленнее (запрашивает фильтры)
- Последующие запросы в течение 6 часов - быстрее (используют кеш)

## Деплой на Render.com

```
Build: pip install -r requirements.txt
Start: uvicorn main:app --host 0.0.0.0 --port $PORT
```

## Лицензия

MIT
