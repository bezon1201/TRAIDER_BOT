# Telegram Bot - Python

Telegram-бот на Python с поддержкой webhook, прокси, хранилища файлов и размещением на Render.com

## Версии

### v2.0 (12.11.2025)
- ✨ Новый модуль `data.py` для управления файлами в хранилище
- 📁 Команда `/data` - показывает список файлов (через запятую)
- 📤 Команда `/data export all` - отправляет все файлы в чат
- 🗑️ Команда `/data delete all` - удаляет все файлы из DATA_STORAGE
- 🔧 Поддержка Render Disk (переменная DATA_STORAGE)
- ✅ Добавлен root endpoint `/` для проверки статуса

### v1.0 (12.11.2025)
- ✅ Базовая структура бота на python-telegram-bot
- ✅ Webhook для работы на Render.com
- ✅ Поддержка прокси для статического IP (HTTP_PROXY, HTTPS_PROXY)
- ✅ Отправка сообщения "Бот запущен" в admin chat при старте
- ✅ Health check endpoint `/health` для UptimeRobot (HEAD/GET)
- ✅ Команда /start

## Требования

- Python 3.9+
- Переменные окружения (см. `.env.example`)

## Установка локально

1. Клонируй репозиторий:
```bash
git clone https://github.com/your-username/telegram-bot.git
cd telegram-bot
```

2. Установи зависимости:
```bash
pip install -r requirements.txt
```

3. Создай файл `.env` на основе `.env.example` и заполни переменные

4. Запусти бота:
```bash
python main.py
```

## Деплой на Render.com

1. Создай новый **Web Service** на [Render.com](https://render.com)
2. Подключи GitHub репозиторий
3. Настройки:
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `gunicorn main:app --bind 0.0.0.0:$PORT`
4. Добавь Environment Variables:
   - `BOT_TOKEN`
   - `ADMIN_CHAT_ID`
   - `WEBHOOK_BASE` (например: `https://your-app.onrender.com`)
   - `HTTP_PROXY` и `HTTPS_PROXY`
   - `DATA_STORAGE` (путь к Render Disk, например: `/var/data`)
   - Все остальные переменные из `.env.example`

### Настройка Render Disk

1. В Render.com настройки Web Service найди раздел "Disks"
2. Создай новый диск с именем (например, `data`)
3. Установи Mount Path: `/var/data`
4. В Environment Variables установи: `DATA_STORAGE=/var/data`

## UptimeRobot мониторинг

1. Создай новый монитор на [UptimeRobot.com](https://uptimerobot.com)
2. Тип: **HTTP(s)** или **HEAD**
3. URL: `https://your-app.onrender.com/health`
4. Интервал проверки: 5 минут

Это предотвратит "засыпание" бесплатного Render web service.

## Структура проекта

```
.
├── main.py              # Основной файл бота с командами
├── data.py              # Модуль для работы с файлами
├── requirements.txt     # Python зависимости
├── Procfile            # Команда запуска для Render
├── .env.example        # Пример переменных окружения
└── README.md           # Документация
```

## Endpoints

- `/health` - Health check для мониторинга (HEAD/GET)
- `/` - Root endpoint (GET/HEAD)
- `/webhook` - Webhook endpoint для Telegram

## Команды бота

- `/start` - Приветственное сообщение
- `/data` - Показать список файлов в хранилище
- `/data export all` - Отправить все файлы в активный чат
- `/data delete all` - Удалить все файлы из DATA_STORAGE

## Использование модуля data.py

```python
from data import DataStorage

# Инициализация
storage = DataStorage('/path/to/storage')

# Получить список файлов
files = storage.get_files_list()

# Сохранить файл
storage.save_file('myfile.txt', b'content')

# Получить путь к файлу
path = storage.get_file_path('myfile.txt')

# Удалить все файлы
storage.delete_all()

# Проверить существование файла
exists = storage.file_exists('myfile.txt')

# Получить размер файла
size = storage.get_file_size('myfile.txt')
```

## Следующие шаги

- Добавление команд для работы с Binance API
- Реализация обработчиков для метрик и расчётов
- Хранилище данных в БД
- Расширенная логика бота

## Лицензия

MIT
