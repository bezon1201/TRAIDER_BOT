# Telegram Bot - FastAPI Version 4.4

Telegram-бот на Python с FastAPI, Uvicorn и полным сбором метрик как в старом боте

## Версия 4.4 (12.11.2025)

✨ **Полная реализация с торговыми фильтрами:**

- 📊 Таймфреймы: **12h, 6h, 4h, 2h** (как в старом боте)
- 📈 Расчет **SMA14** и **ATR14** для каждого таймфрейма
- 🎯 **Торговые фильтры** (кешируются на 6 часов):
  - **tickSize** — минимальный шаг цены
  - **minQty** / **maxQty** — минимальный/максимальный объем
  - **minNotional** — минимальная стоимость сделки
  - **stepSize** — шаг изменения объема
  - **maxNumOrders** — макс. активных ордеров
  - **maxNumAlgoOrders** — макс. алгоритмических ордеров
  - **icebergQty** — минимальный объем айсберг ордеров
- 💾 Метрики сохраняются в JSON для каждой пары
- 🔇 Сбор происходит молча без сообщений в Telegram

## Команды бота

### `/start`
Приветствие

### `/coins <pair1> <pair2> ...`
Добавляет пары для сбора метрик

### `/now`
Собирает полные метрики:
- Текущая цена (ticker)
- Торговые фильтры (с кешем)
- Свечи для 12h, 6h, 4h, 2h
- SMA14 и ATR14 для каждого таймфрейма

### `/data`
Список файлов в хранилище

### `/data export all`
Отправляет все файлы в чат

### `/data delete all`
Удаляет все файлы

## Структура JSON файла

**Пример BTCUSDT.json:**
```json
{
  "symbol": "BTCUSDT",
  "timestamp": "2025-12-12T06:47:00+00:00",
  "ticker": {
    "price": 42500.00,
    "bid_price": 42499.90,
    "ask_price": 42500.10,
    "high": 43000.00,
    "low": 42000.00,
    "volume": 15000.5,
    "trades": 500000
  },
  "filters": {
    "price_filter": {
      "min_price": 0.01,
      "max_price": 1000000,
      "tick_size": 0.01
    },
    "lot_size": {
      "min_qty": 0.00001,
      "max_qty": 10000,
      "step_size": 0.00001
    },
    "min_notional": {
      "min_notional": 10
    },
    "max_num_orders": {
      "limit": 200
    },
    "max_num_algo_orders": {
      "limit": 10
    }
  },
  "timeframes": {
    "12h": {
      "klines": [...],
      "indicators": {"sma14": 42100.50, "atr14": 350.25}
    },
    "6h": {...},
    "4h": {...},
    "2h": {...}
  }
}
```

## Кеширование

Торговые фильтры кешируются на **6 часов**, что снижает нагрузку на API Binance и ускоряет сбор метрик.

## Деплой на Render.com

- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `uvicorn main:app --host 0.0.0.0 --port $PORT`

## Следующие шаги (версия 5.0)

- Определение рыночного режима (Up/Down/Range)
- Вычисление торговых флагов
- Периодический сбор метрик по расписанию
- История метрик

## Лицензия

MIT
